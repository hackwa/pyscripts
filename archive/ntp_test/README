Command to run Server:

python3 server.py

Command to run client:

python3 client.py <IP address of Server>

Programs need direct network access between server and client to work. 
If that is not available then some kind of port forwarding is needed.
